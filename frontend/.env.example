# API Configuration
# For local development, point to backend on port 3001
VITE_API_URL=http://localhost:3001/api

# For production (Docker), nginx reverse proxy handles routing
# Production uses: VITE_API_URL=/api (relative path)
